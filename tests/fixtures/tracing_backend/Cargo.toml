[package]
name = "tracing-backend-fixture"
version = "0.1.0"
edition = "2021"

[dependencies]
exec_time = { path = "../../../", features = ["tracing"] }
tracing = "0.1.41"
tracing-subscriber = { version = "0.3.20", default-features = false, features = ["fmt", "std"] }
